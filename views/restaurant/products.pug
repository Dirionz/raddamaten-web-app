extends ../layout

block content
  h1 Hello World!
  p.lead #{title}
  hr
  .row
    each product in products
        .col-sm-6
            h2 #{product.name}
            p #{product.description}
            p #{product.quantity}
            p #{product.price} sek
            p
              a.btn.btn-default(href='#', role='button') Select »